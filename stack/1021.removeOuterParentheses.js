/**
 * @param {string} S
 * @return {string}
 */
var removeOuterParentheses = function(S) {
    let map = new Map();
    let result = []
    map.set('(', ')');
    map.set(')', '(');
    let arr = S.split('')

    return result.join()
};